services:
  - type: web
    name: kitsune-engine
    runtime: docker
    plan: free
    envVars:
      - key: RUST_LOG
        value: info
      - key: PORT
        value: 8080
    healthCheckPath: /health
